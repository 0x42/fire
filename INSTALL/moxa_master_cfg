#!/bin/sh

nodes=`cat $1`

pthm=/home/tabr

ftpfile=ftp.txt
ftplog=ftp.log

echo "ascii" > $ftpfile
echo "cd $pthm" >> $ftpfile
echo "put master.cfg" >> $ftpfile
echo "exit" >> $ftpfile
chmod 640 $ftpfile

for node in $nodes
do
	ftp $node < $ftpfile > ftplog
	echo "master.cfg ($node) ... ok"
done
